# FactoryIO-Sorting-Control
A Factory I/O sorting system using SR flip-flop logic. It syncs a diffuse sensor, pneumatic pusher, and conveyor so the belt pauses while sorting. This prevents jams by restarting only after the pusher hits its limit and clears the path. Smart, simple, and automated.
